{
  "name": "@nexusloop/types",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts src/auth.types.ts --format cjs,esm --dts --no-config",
    "dev": "tsup src/index.ts src/auth.types.ts --format cjs,esm --dts --watch --no-config",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
} 